{
  "name": "itachiotp",
  "version": "1.1.0",
  "private": true,
  "description": "Fetch CR API stats and forward new OTP values to Telegram",
  "scripts": {
    "start": "node app.js",
    "test": "node --test tests/**/*.test.js"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=10.0.0"
  },
  "packageManager": "npm@10.9.2",
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
